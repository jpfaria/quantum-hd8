# quantum-hd8 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Lib Python + CLI `quantum-hd8` + skill do Claude que controlam tudo da PreSonus Quantum HD 8 pelo `ucdaemon`, e mapeiam o roteamento de re-amp que o Universal Control não mostra.

**Architecture:** Camada A = cliente UCNet em TCP `127.0.0.1:59791` (o mesmo que o app UC usa); o mapa de parâmetros sai do `quantumusbdefs.xml` que o próprio UC instala. Camada B = análise estática de `hwaccess/quantumusbdevice.dylib` atrás do roteamento de re-amp. O codec é puro (sem I/O) e testado com fixtures capturadas ao vivo.

**Tech Stack:** Python ≥ 3.11, stdlib (`socket`, `zlib`, `struct`, `json`, `xml.etree`), pytest. Sem dependência externa na lib.

**Spec:** `docs/superpowers/specs/2026-09-18-quantum-hd8-design.md`

## Global Constraints

- Python ≥ 3.11; pacote `quantum_hd8`; console script `quantum-hd8`; licença MIT.
- Daemon: `127.0.0.1:59791` (TCP). UDP de descoberta: `47809`.
- Fonte do mapa: `/Applications/Universal Control.app/Contents/PlugIns/quantumpanel.bundle/Contents/Resources/quantumusbdefs.xml`, template `QuantumHd8`.
- Escrita em parâmetro não verificado: um por vez, com releitura; todo `set` grava o valor anterior no journal de undo.
- Proibido em qualquer task/subagente: `launchctl`, parar/reiniciar `ucdaemon`, `sudo`, escrita fora do repo, do scratchpad e de `~/.quantum-hd8/`.
- Teste que toca a interface real = marcado `@pytest.mark.device`, fora do run padrão; roda só na sessão principal, nunca em subagente.
- Todo commit é seguido de `git push` (CLAUDE.md do repo).
- Datas no formato DD/MM (2026) nos docs.

## Mapa de arquivos

| Arquivo | Responsabilidade |
|---|---|
| `pyproject.toml` | pacote, script, pytest (marker `device` desligado por padrão) |
| `CLAUDE.md` | regras do repo (push, segurança) |
| `quantum_hd8/__init__.py` | versão |
| `quantum_hd8/defs.py` | lê `quantumusbdefs.xml` → lista de `Param` (caminho, tipo, faixa, flags) |
| `quantum_hd8/params.json` | mapa gerado por `tools/gen_params.py` (com `verified`/`hidden`) |
| `quantum_hd8/ucnet.py` | codec UCNet: `encode(msg)`, `Decoder.feed(bytes) -> list[Message]` |
| `quantum_hd8/client.py` | `Client`: connect, hello, subscribe, keepalive, estado, `get`, `set`, eventos |
| `quantum_hd8/undo.py` | journal de undo em `~/.quantum-hd8/undo.jsonl` |
| `quantum_hd8/cli.py` | comandos |
| `tools/gen_params.py` | gera `params.json` |
| `tools/probe.py` | captura bytes crus do daemon para `tests/fixtures/` |
| `tools/diff_state.py` | diff entre dois `dump` |
| `tests/…` | testes offline + `device` |
| `docs/protocol.md` | protocolo byte a byte |
| `skills/quantum-hd8/SKILL.md`, `.claude-plugin/*.json` | plugin |

---

### Task 1: Scaffold do pacote

**Files:**
- Create: `pyproject.toml`, `CLAUDE.md`, `README.md`, `.gitignore`, `quantum_hd8/__init__.py`, `quantum_hd8/cli.py`, `tests/test_cli.py`

**Interfaces:**
- Produces: `quantum_hd8.cli.main(argv: list[str] | None = None) -> int`; subcomandos registrados em `build_parser() -> argparse.ArgumentParser`.

- [ ] **Step 1: teste que falha**

```python
# tests/test_cli.py
from quantum_hd8.cli import main

def test_version(capsys):
    assert main(["--version"]) == 0
    assert "quantum-hd8" in capsys.readouterr().out
```

- [ ] **Step 2:** `python3 -m pytest -q` → FAIL (`ModuleNotFoundError`).

- [ ] **Step 3: implementação**

```toml
# pyproject.toml
[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "quantum-hd8"
version = "0.1.0"
description = "Control a PreSonus Quantum HD 8 without Universal Control: UCNet client, CLI, Claude Code skill."
readme = "README.md"
requires-python = ">=3.11"
license = {text = "MIT"}

[project.optional-dependencies]
dev = ["pytest"]

[project.scripts]
quantum-hd8 = "quantum_hd8.cli:main"

[tool.setuptools.packages.find]
include = ["quantum_hd8*"]

[tool.setuptools.package-data]
quantum_hd8 = ["params.json"]

[tool.pytest.ini_options]
markers = ["device: touches the real Quantum HD 8 via ucdaemon"]
addopts = "-m 'not device'"
```

```python
# quantum_hd8/__init__.py
__version__ = "0.1.0"
```

```python
# quantum_hd8/cli.py
import argparse
import sys

from . import __version__


def build_parser() -> argparse.ArgumentParser:
    p = argparse.ArgumentParser(prog="quantum-hd8")
    p.add_argument("--version", action="version", version=f"quantum-hd8 {__version__}")
    p.add_subparsers(dest="cmd")
    return p


def main(argv: list[str] | None = None) -> int:
    p = build_parser()
    try:
        args = p.parse_args(argv)
    except SystemExit as e:
        return int(e.code or 0)
    if not args.cmd:
        p.print_help()
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

`.gitignore`: `__pycache__/`, `*.egg-info/`, `.pytest_cache/`, `*.wav`.

`CLAUDE.md`: copiar a seção "Every change is committed AND pushed" do `../mvave/CLAUDE.md` trocando `mvave` por `quantum-hd8`, mais a seção de segurança da spec (um parâmetro por vez, nada de `launchctl`/reiniciar daemon, subagente não toca a interface).

`README.md`: uma linha de descrição + `pipx install git+https://github.com/jpfaria/quantum-hd8`.

- [ ] **Step 4:** `pip install -e '.[dev]' && python3 -m pytest -q` → PASS.
- [ ] **Step 5:** `git add -A && git commit -m "scaffold: pacote quantum_hd8 + CLI vazia" && git push`

---

### Task 2: Mapa de parâmetros a partir do `quantumusbdefs.xml`

**Files:**
- Create: `quantum_hd8/defs.py`, `tools/gen_params.py`, `quantum_hd8/params.json` (gerado), `tests/fixtures/defs_min.xml`, `tests/test_defs.py`

**Interfaces:**
- Produces: `defs.Param` (dataclass: `path: str`, `id: str`, `name: str`, `type: str`, `min: float|None`, `max: float|None`, `default: str|None`, `units: str|None`, `flags: list[str]`, `component: str`); `defs.load_template(xml_path: str, template: str = "QuantumHd8") -> list[Param]`; `params.json` = lista de dicts de `Param` + `verified: false`, `hidden: false`.

Regras do XML (lidas em 18/09): `ParamList` tem `inherit` (herda os params do pai, pai primeiro) e `mixin` (acrescenta os params da lista); `Template` tem `Component` aninhado com `paramlist` e `uc:foreach id="$i" min max`. O caminho é `<componente>/<subcomponente>/<param>`, p.ex. `line/ch1/preampgain`, `aux/ch13/volume`, `global/phones2_src`. O namespace `uc:` precisa ser tratado (usar `{*}Tag` do ElementTree ≥ 3.8).

- [ ] **Step 1: fixture mínima** `tests/fixtures/defs_min.xml`, copiando a estrutura real:

```xml
<?xml version="1.0" encoding="UTF-8"?>
<uc:Definitions xmlns:uc="urn:presonus:uc">
 <uc:Parameters>
  <uc:ParamList id="Global" starttag="1">
   <uc:Param id="dim" name="Main Output Dim" type="toggle" def="0" flags="storable"/>
  </uc:ParamList>
  <uc:ParamList id="HeadPhones2" starttag="50">
   <uc:Param id="phones2_src" name="Phones 2 Source" type="list" units="EmptyParamList" def="0" flags="mutable"/>
  </uc:ParamList>
  <uc:ParamList id="GlobalQHD8" starttag="30" inherit="Global" mixin="HeadPhones2"/>
  <uc:ParamList id="Channel" starttag="100">
   <uc:Param id="volume" name="Volume" type="float" min="-96" max="10" def="-96" units="gain" flags="storable linkable mutable"/>
  </uc:ParamList>
  <uc:ParamList id="Mic" starttag="140" inherit="Channel">
   <uc:Param id="preampgain" name="Preamp Gain" type="float" min="0" max="75" def="0" units="gain.0" flags="storable mutable"/>
  </uc:ParamList>
 </uc:Parameters>
 <uc:Templates>
  <uc:Template id="QuantumHd8" flags="public">
   <uc:Component id="global" name="Global" paramlist="GlobalQHD8"/>
   <uc:Component id="line" name="Inputs">
    <uc:foreach id="$i" min="1" max="2">
     <uc:Component id="ch$i" paramlist="Mic"/>
    </uc:foreach>
   </uc:Component>
  </uc:Template>
 </uc:Templates>
</uc:Definitions>
```

Antes de escrever: `head -5` do XML real para copiar o `xmlns:uc` exato para a fixture.

- [ ] **Step 2: teste que falha**

```python
# tests/test_defs.py
from pathlib import Path
from quantum_hd8.defs import load_template

FX = Path(__file__).parent / "fixtures" / "defs_min.xml"

def test_paths_inherit_mixin_foreach():
    params = {p.path: p for p in load_template(str(FX))}
    assert set(params) == {
        "global/dim", "global/phones2_src",
        "line/ch1/volume", "line/ch1/preampgain",
        "line/ch2/volume", "line/ch2/preampgain",
    }
    g = params["line/ch2/preampgain"]
    assert (g.type, g.min, g.max, g.units) == ("float", 0.0, 75.0, "gain.0")
    assert g.flags == ["storable", "mutable"]
    assert g.component == "line/ch2"
```

- [ ] **Step 3:** rodar → FAIL.

- [ ] **Step 4: implementação**

```python
# quantum_hd8/defs.py
"""Read the parameter tree the Universal Control app ships in quantumusbdefs.xml."""
from dataclasses import dataclass, field, asdict
import xml.etree.ElementTree as ET

DEFS_XML = ("/Applications/Universal Control.app/Contents/PlugIns/"
            "quantumpanel.bundle/Contents/Resources/quantumusbdefs.xml")


@dataclass
class Param:
    path: str
    id: str
    name: str
    type: str
    min: float | None
    max: float | None
    default: str | None
    units: str | None
    flags: list[str] = field(default_factory=list)
    component: str = ""

    def to_dict(self) -> dict:
        return asdict(self)


def _num(v):
    return float(v) if v is not None else None


def _lists(root) -> dict[str, ET.Element]:
    return {pl.get("id"): pl for pl in root.iter("{*}ParamList")}


def _params_of(lists, list_id: str) -> list[ET.Element]:
    pl = lists[list_id]
    out = []
    if pl.get("inherit"):
        out += _params_of(lists, pl.get("inherit"))
    out += list(pl.findall("{*}Param"))
    if pl.get("mixin"):
        out += _params_of(lists, pl.get("mixin"))
    return out


def _walk(node, prefix, lists, out):
    for child in node:
        tag = child.tag.split("}")[-1]
        if tag == "foreach":
            var = child.get("id")
            for i in range(int(child.get("min")), int(child.get("max")) + 1):
                _walk_expanded(child, prefix, lists, out, var, str(i))
        elif tag == "Component":
            _component(child, child.get("id"), prefix, lists, out, None, None)


def _walk_expanded(node, prefix, lists, out, var, val):
    for child in node:
        if child.tag.split("}")[-1] == "Component":
            _component(child, child.get("id").replace(var, val), prefix, lists, out, var, val)


def _component(el, cid, prefix, lists, out, var, val):
    path = f"{prefix}/{cid}" if prefix else cid
    if el.get("paramlist"):
        for p in _params_of(lists, el.get("paramlist")):
            out.append(Param(
                path=f"{path}/{p.get('id')}", id=p.get("id"), name=p.get("name") or "",
                type=p.get("type"), min=_num(p.get("min")), max=_num(p.get("max")),
                default=p.get("def"), units=p.get("units"),
                flags=(p.get("flags") or "").split(), component=path))
    _walk(el, path, lists, out)


def load_template(xml_path: str = DEFS_XML, template: str = "QuantumHd8") -> list[Param]:
    root = ET.parse(xml_path).getroot()
    lists = _lists(root)
    tpl = next(t for t in root.iter("{*}Template") if t.get("id") == template)
    out: list[Param] = []
    _walk(tpl, "", lists, out)
    return out
```

- [ ] **Step 5:** rodar → PASS.

- [ ] **Step 6: gerador**

```python
# tools/gen_params.py
"""Generate quantum_hd8/params.json from the installed Universal Control defs.
Keeps verified/hidden flags already set in the existing params.json."""
import json
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
from quantum_hd8.defs import DEFS_XML, load_template  # noqa: E402

OUT = Path(__file__).resolve().parents[1] / "quantum_hd8" / "params.json"

old = {p["path"]: p for p in json.loads(OUT.read_text())} if OUT.exists() else {}
rows = []
for p in load_template(sys.argv[1] if len(sys.argv) > 1 else DEFS_XML):
    d = p.to_dict()
    prev = old.get(p.path, {})
    d["verified"] = prev.get("verified", False)
    d["hidden"] = prev.get("hidden", False)
    rows.append(d)
OUT.write_text(json.dumps(rows, indent=1, ensure_ascii=False) + "\n")
print(f"{len(rows)} params -> {OUT}")
```

Rodar `python3 tools/gen_params.py`. Conferir no output: há `line/ch1..36`, `aux/ch1..14`, `main/ch1`, `global/*`. Componente `presets` não tem paramlist e não gera param (tratado na Task 7).

- [ ] **Step 7:** `git add -A && git commit -m "defs: mapa de parâmetros da HD 8 a partir do quantumusbdefs.xml" && git push`

---

### Task 3: Probe ao vivo — capturar os bytes do daemon (sessão principal)

Esta task **toca o daemon real** (só leitura). Não despachar para subagente.

**Files:**
- Create: `tools/probe.py`, `tests/fixtures/probe-*.bin`, `docs/protocol.md` (rascunho)

**Interfaces:**
- Produces: fixtures cruas `tests/fixtures/probe-hello.bin` (o que mandamos), `tests/fixtures/probe-rx.bin` (tudo que o daemon devolveu nos primeiros 5 s), e `docs/protocol.md` com o formato **observado**.

Hipótese a testar (formato do StudioLive, projetos públicos): cada pacote é `b"UC\x00\x01"` + `uint16 LE size` + `code(2 ASCII)` + `cbytes(4)` + payload, e `size = 6 + len(payload)`. Códigos: `UM` hello, `JM` JSON (`uint32 LE len` + JSON), `KA` keepalive, `PV` valor, `PS` string, `ZM`/`ZB` estado comprimido com zlib. Se o daemon não responder com essa hipótese, o probe primeiro só **escuta** (conecta e lê 5 s sem mandar nada) e o formato vem do que ele mandar.

- [ ] **Step 1: probe**

```python
# tools/probe.py
"""Read-only probe of ucdaemon: connect, optionally say hello + subscribe, dump raw bytes."""
import json
import socket
import struct
import sys
import time
from pathlib import Path

FX = Path(__file__).resolve().parents[1] / "tests" / "fixtures"
CB = b"\x68\x00\x65\x00"


def pkt(code: bytes, payload: bytes) -> bytes:
    return b"UC\x00\x01" + struct.pack("<H", 6 + len(payload)) + code + CB + payload


def jm(obj) -> bytes:
    body = json.dumps(obj).encode()
    return pkt(b"JM", struct.pack("<I", len(body)) + body)


def main(mode: str):
    s = socket.create_connection(("127.0.0.1", 59791), timeout=2)
    sent = b""
    if mode == "subscribe":
        sent += pkt(b"UM", b"\x00\x00" + struct.pack("<H", 47809))
        sent += jm({"id": "Subscribe", "clientName": "quantum-hd8",
                    "clientInternalName": "quantumhd8", "clientType": "Mac",
                    "clientDescription": "quantum-hd8 probe",
                    "clientIdentifier": "quantum-hd8-probe",
                    "clientOptions": "perm users levl redu rtan", "clientEncoding": 23117})
        s.sendall(sent)
    rx = b""
    end = time.time() + 5
    while time.time() < end:
        try:
            chunk = s.recv(65536)
        except socket.timeout:
            continue
        if not chunk:
            break
        rx += chunk
    s.close()
    FX.mkdir(parents=True, exist_ok=True)
    (FX / f"probe-{mode}-tx.bin").write_bytes(sent)
    (FX / f"probe-{mode}-rx.bin").write_bytes(rx)
    print(f"sent {len(sent)} B, got {len(rx)} B")
    print(rx[:256])


if __name__ == "__main__":
    main(sys.argv[1] if len(sys.argv) > 1 else "listen")
```

- [ ] **Step 2:** `python3 tools/probe.py listen` → registrar o que o daemon manda sem estímulo.
- [ ] **Step 3:** `python3 tools/probe.py subscribe` → esperado: começa com `UC\x00\x01` e contém `ZM`/`ZB` (estado) ou `JM`. Se não bater, ler `strings -n 3` do `ucnet.framework` do daemon (`/Library/Application Support/Presonus/universalcontrol/ucdaemon.bundle/Contents/Frameworks/ucnet.framework`) atrás dos códigos de 2 letras e do JSON de subscribe, ajustar o probe e repetir. Critério de saída: um pacote de estado decodificado à mão (zlib) contendo nomes de `params.json`, p.ex. `preampgain`.
- [ ] **Step 4:** escrever em `docs/protocol.md` o formato **observado** (header, size, códigos vistos, cbytes, JSON de subscribe que funcionou, formato do estado), com a data 18/09 e o que foi hipótese × medido.
- [ ] **Step 5:** `git add -A && git commit -m "probe: captura crua do ucdaemon + protocolo observado" && git push`

---

### Task 4: Codec UCNet (`ucnet.py`) testado com as fixtures

**Files:**
- Create: `quantum_hd8/ucnet.py`, `tests/test_ucnet.py`

**Interfaces:**
- Consumes: fixtures da Task 3; formato em `docs/protocol.md`.
- Produces:
  - `Message` (dataclass: `code: str`, `cbytes: bytes`, `payload: bytes`)
  - `encode(code: str, payload: bytes, cbytes: bytes = CB) -> bytes`
  - `Decoder().feed(data: bytes) -> list[Message]` (bufferiza pacote partido)
  - `json_payload(obj) -> bytes` / `parse_json(msg) -> dict`
  - `pv_payload(path: str, value: float) -> bytes` / `parse_pv(msg) -> tuple[str, float]`
  - `parse_state(msg) -> dict` (ZM/ZB → dict da árvore)

Se a Task 3 mediu um formato diferente da hipótese, **o que vale é o `docs/protocol.md`**; ajustar constantes e testes, não a fixture.

- [ ] **Step 1: testes que falham**

```python
# tests/test_ucnet.py
from pathlib import Path
from quantum_hd8 import ucnet

FX = Path(__file__).parent / "fixtures"


def test_roundtrip_ka():
    raw = ucnet.encode("KA", b"")
    [m] = ucnet.Decoder().feed(raw)
    assert (m.code, m.payload) == ("KA", b"")


def test_split_packet_is_buffered():
    raw = ucnet.encode("JM", ucnet.json_payload({"id": "X"}))
    d = ucnet.Decoder()
    assert d.feed(raw[:5]) == []
    [m] = d.feed(raw[5:])
    assert ucnet.parse_json(m) == {"id": "X"}


def test_pv_roundtrip():
    m = ucnet.Decoder().feed(ucnet.encode("PV", ucnet.pv_payload("line/ch1/volume", -12.5)))[0]
    assert ucnet.parse_pv(m) == ("line/ch1/volume", -12.5)


def test_live_subscribe_capture_has_state_with_known_params():
    msgs = ucnet.Decoder().feed((FX / "probe-subscribe-rx.bin").read_bytes())
    states = [ucnet.parse_state(m) for m in msgs if m.code in ("ZM", "ZB")]
    assert states, [m.code for m in msgs]
    flat = str(states[0])
    assert "preampgain" in flat
```

- [ ] **Step 2:** rodar → FAIL.

- [ ] **Step 3: implementação** (formato da hipótese; trocar pelo medido se diferir)

```python
# quantum_hd8/ucnet.py
"""UCNet framing (the protocol Universal Control speaks to ucdaemon). Pure, no I/O."""
from dataclasses import dataclass
import json
import struct
import zlib

MAGIC = b"UC\x00\x01"
CB = b"\x68\x00\x65\x00"


@dataclass
class Message:
    code: str
    cbytes: bytes
    payload: bytes


def encode(code: str, payload: bytes, cbytes: bytes = CB) -> bytes:
    return MAGIC + struct.pack("<H", 6 + len(payload)) + code.encode() + cbytes + payload


class Decoder:
    def __init__(self):
        self._buf = b""

    def feed(self, data: bytes) -> list[Message]:
        self._buf += data
        out = []
        while True:
            i = self._buf.find(MAGIC)
            if i < 0:
                self._buf = self._buf[-3:]
                return out
            self._buf = self._buf[i:]
            if len(self._buf) < 6:
                return out
            size = struct.unpack_from("<H", self._buf, 4)[0]
            if len(self._buf) < 6 + size:
                return out
            body, self._buf = self._buf[6:6 + size], self._buf[6 + size:]
            out.append(Message(body[:2].decode("latin1"), body[2:6], body[6:]))


def json_payload(obj) -> bytes:
    b = json.dumps(obj).encode()
    return struct.pack("<I", len(b)) + b


def parse_json(m: Message) -> dict:
    n = struct.unpack_from("<I", m.payload)[0]
    return json.loads(m.payload[4:4 + n])


def pv_payload(path: str, value: float) -> bytes:
    return path.encode() + b"\x00\x00\x00" + struct.pack("<f", value)


def parse_pv(m: Message) -> tuple[str, float]:
    key, _, rest = m.payload.partition(b"\x00")
    return key.decode(), round(struct.unpack("<f", rest[-4:])[0], 4)


def parse_state(m: Message) -> dict:
    n = struct.unpack_from("<I", m.payload)[0]
    raw = zlib.decompress(m.payload[4:4 + n] if n <= len(m.payload) - 4 else m.payload[4:])
    try:
        return json.loads(raw)
    except ValueError:
        return {"_raw": raw.decode("latin1")}
```

Nota: `Decoder` com pacote maior que 65535 B (estado grande) — se a Task 3 mostrar estado dividido em vários pacotes, remontar antes do `zlib` e cobrir com teste.

- [ ] **Step 4:** rodar → PASS.
- [ ] **Step 5:** `git add -A && git commit -m "ucnet: codec + testes com captura real" && git push`

---

### Task 5: Cliente só leitura + `dump`, `get`, `state`, `listen`

**Files:**
- Create: `quantum_hd8/client.py`, `tests/test_client.py`
- Modify: `quantum_hd8/cli.py`

**Interfaces:**
- Consumes: `ucnet.*`.
- Produces:
  - `Client(host="127.0.0.1", port=59791, sock_factory=socket.create_connection)` — `sock_factory` injetável para teste.
  - `Client.connect() -> dict` (faz hello + subscribe, devolve o estado inicial já achatado)
  - `Client.state: dict[str, object]` (achatado, chaves = caminhos `line/ch1/volume`)
  - `Client.get(path: str) -> object`
  - `Client.events(timeout: float | None) -> Iterator[tuple[str, object]]` (PV/PS recebidos; atualiza `state`)
  - `Client.close()`; keepalive `KA` a cada 1 s num thread daemon.
  - `flatten(tree: dict, prefix="") -> dict[str, object]`

- [ ] **Step 1: teste com socket falso**

```python
# tests/test_client.py
import zlib, json, struct
from quantum_hd8 import ucnet
from quantum_hd8.client import Client, flatten


class FakeSock:
    def __init__(self, rx: bytes):
        self.rx, self.tx = [rx], b""
    def sendall(self, b): self.tx += b
    def recv(self, n): return self.rx.pop(0) if self.rx else b""
    def settimeout(self, t): pass
    def close(self): pass


def state_pkt(tree):
    z = zlib.compress(json.dumps(tree).encode())
    return ucnet.encode("ZM", struct.pack("<I", len(z)) + z)


def test_flatten():
    assert flatten({"line": {"ch1": {"volume": -3}}}) == {"line/ch1/volume": -3}


def test_connect_reads_state_and_get():
    tree = {"line": {"ch1": {"preampgain": 12.0}}}
    fake = FakeSock(state_pkt(tree))
    c = Client(sock_factory=lambda *a, **k: fake)
    c.connect()
    assert c.get("line/ch1/preampgain") == 12.0
    assert b"Subscribe" in fake.tx
```

O formato real da árvore sai da Task 3 (pode vir com `children`/`values` em vez de dict aninhado simples). `flatten` segue o formato medido; o teste usa a mesma forma que a fixture real — copiar um trecho de `probe-subscribe-rx.bin` decodificado se diferir.

- [ ] **Step 2:** FAIL. **Step 3:** implementar `Client` (hello e subscribe exatamente como o probe que funcionou; ler até o primeiro `ZM`/`ZB`; timeout de 3 s com erro claro "ucdaemon não respondeu — o Universal Control está instalado e o daemon rodando?").
- [ ] **Step 4:** PASS.
- [ ] **Step 5: CLI**
  - `quantum-hd8 dump [--out f.json]` → estado achatado em JSON (ordenado).
  - `quantum-hd8 get <path>` → valor; caminho desconhecido = erro listando os 5 mais parecidos (`difflib.get_close_matches`).
  - `quantum-hd8 state` → resumo legível: ganhos dos pré, 48V, mutes, fontes de fone/S-PDIF, volume da main.
  - `quantum-hd8 listen` → imprime `path = valor` a cada evento, até Ctrl-C.
  Testes de CLI com `Client` falso via monkeypatch de `quantum_hd8.cli.Client`.
- [ ] **Step 6 (device, sessão principal):** `quantum-hd8 dump --out /tmp/…/a.json`; conferir que `line/ch<N>/preampgain` bate com o que o UC mostra. Registrar em `docs/protocol.md`.
- [ ] **Step 7:** commit + push.

---

### Task 6: `diff_state` + descoberta das escritas

**Files:**
- Create: `tools/diff_state.py`, `tests/test_diff_state.py`, `tests/fixtures/uc-write-*.bin`

**Interfaces:**
- Produces: `diff(a: dict, b: dict) -> list[tuple[str, object, object]]` (caminho, antes, depois).

- [ ] **Step 1: teste**

```python
from tools.diff_state import diff

def test_diff():
    assert diff({"a": 1, "b": 2}, {"a": 1, "b": 3, "c": 4}) == [("b", 2, 3), ("c", None, 4)]
```

(adicionar `tests/conftest.py` com `sys.path.insert(0, <raiz>)` para importar `tools`.)

- [ ] **Step 2–4:** FAIL → implementar (`sorted(set(a)|set(b))`, só diferentes) → PASS. CLI do script: `python3 tools/diff_state.py a.json b.json`.
- [ ] **Step 5 (device):** com `quantum-hd8 listen` rodando e gravando os bytes crus (`--raw tests/fixtures/uc-write-<param>.bin`), o João mexe **um** controle no UC: fader do `aux/ch13` (loopback, não chega nos gabinetes). O daemon repassa a mudança aos inscritos: essa é a forma da escrita. Anotar em `docs/protocol.md` (código, cbytes, formato do valor — float normalizado 0..1 ou dB).
- [ ] **Step 6:** commit + push.

---

### Task 7: Escrita: `set` + `undo`, cenas (`presets`)

**Files:**
- Create: `quantum_hd8/undo.py`, `tests/test_undo.py`, `tests/test_set.py`
- Modify: `quantum_hd8/client.py`, `quantum_hd8/cli.py`, `quantum_hd8/params.json`

**Interfaces:**
- Consumes: formato de escrita medido na Task 6.
- Produces:
  - `Client.set(path: str, value) -> object` — manda PV/PS, espera o eco do daemon (≤ 1 s) e devolve o valor relido; levanta `WriteNotConfirmed` se não vier eco.
  - `undo.record(path, before, after, journal=Path.home()/".quantum-hd8/undo.jsonl")`, `undo.pop(journal=...) -> tuple[str, object] | None`.
  - Validação por `params.json`: tipo, faixa min/max, `readonly` recusa.
  - CLI: `set <path> <valor>`, `undo`, `scene list|load <nome>|save <nome>`.

- [ ] **Step 1: testes offline** — `test_set.py` com `FakeSock` que ecoa o PV recebido: `set` grava no journal (`tmp_path`), valida faixa (`line/ch1/preampgain 80` → `ValueError`), recusa `readonly` (`line/ch1/clip`). `test_undo.py`: record+pop LIFO.
- [ ] **Step 2–4:** FAIL → implementar → PASS.
- [ ] **Step 5 (device):** `quantum-hd8 set aux/ch13/volume -20`, conferir no UC, `quantum-hd8 undo`. Marcar `verified: true` em `params.json` para esse param só depois de ver o eco **e** o UC mudar.
- [ ] **Step 6: cenas.** O componente `presets` não tem paramlist no XML: a API sai do estado (`dump` sob `presets/`) e do que o UC manda quando o João dá duplo clique numa cena com `listen --raw` gravando. Implementar `scene list/load/save` com o formato medido; teste offline com a fixture capturada. **Lembrete da doc do rig:** carregar cena zera os ganhos dos pré — `scene load` imprime os ganhos antes/depois e oferece `--keep-gains` (regrava os ganhos anteriores, um por vez).
- [ ] **Step 7:** commit + push.

---

### Task 8: Atalhos `preamp` e `route`

**Files:**
- Modify: `quantum_hd8/cli.py`; Create: `tests/test_shortcuts.py`

**Interfaces:**
- Consumes: `Client.set`.
- Produces: `preamp <ch> gain <dB>` → `line/ch<ch>/preampgain`; `preamp <ch> phantom on|off` → `line/ch<ch>/48v`; `preamp <ch> pad|hpf on|off`; `route phones1|phones2|spdif <fonte>` → `global/phones1_src` (nome real conferido no `params.json`)/`global/phones2_src`/`global/spdif_src`. As fontes são listas (`units="EmptyParamList"`, preenchidas em runtime): pegar os rótulos do estado (Task 5) e aceitar o rótulo (`"Loopback 1"`) ou o índice.

- [ ] **Step 1: testes** — mapeamento de argumento → caminho/valor, com `Client` falso registrando `set`. Rótulo inexistente → erro listando os válidos.
- [ ] **Step 2–4:** FAIL → implementar → PASS.
- [ ] **Step 5:** commit + push.

---

### Task 9: Medidores

**Files:**
- Modify: `quantum_hd8/client.py`, `quantum_hd8/cli.py`; Create: `tests/test_meters.py`, `tests/fixtures/meters-*.bin`

- [ ] **Step 1 (device):** descobrir por onde vêm os medidores. Hipótese (StudioLive): o daemon manda `MS` por **UDP** para a porta informada no `UM`. Probe: abrir UDP numa porta livre, anunciar essa porta no `UM`, subscribe, gravar 3 s em `tests/fixtures/meters-udp.bin`; também gravar o TCP. Registrar em `docs/protocol.md` qual canal trouxe e o layout (quantos valores, ordem, escala). Validar o layout com `music-setup/tools/tone` tocando em um canal conhecido.
- [ ] **Step 2: teste** — `parse_meters(fixture) -> dict[str, float]` com o canal do tom em nível alto e os outros baixos.
- [ ] **Step 3–4:** implementar → PASS.
- [ ] **Step 5:** CLI `meters` (linha por canal com dBFS, atualiza 10×/s; `--once` imprime um snapshot JSON).
- [ ] **Step 6:** commit + push.

---

### Task 10: Camada B — roteamento de re-amp escondido

**Files:**
- Create: `docs/camada-b-reamp.md`; possivelmente `quantum_hd8/params.json` (`hidden: true`)

- [ ] **Step 1: estático.** `strings -n 4` e `nm -gU` de `hwaccess/quantumusbdevice.dylib` e `quantumdevice.dylib`: localizar `Reamp 1`, `Reamp 2`, `Reamp Out`, `QuantumUsbHD8DawOutputComponent`, `SendCommand`, `SendBulkData`. Com `otool -tV` (ou `objdump -d`) achar as funções que referenciam essas strings e os códigos de comando que elas passam para `SendCommand`. Anotar tudo em `docs/camada-b-reamp.md` separando **lido no binário** de **hipótese**.
- [ ] **Step 2: estado.** Procurar no `dump` (Task 5) qualquer chave com `reamp`/`Reamp` ou fonte de roteamento que não está no XML; o daemon pode expor parâmetros além do XML.
- [ ] **Step 3: prova (device, sessão principal, com o João avisado).** Só se o passo 1 ou 2 achar um parâmetro. Antes de escrever: pedir ao João para baixar o volume do caminho re-amp (ADA#2 Out 1 → reamp B → pedais → SYN-2 → SYN-5050). Um `set` por vez. Medir com `tone` em Loopback/um bus e `rec` no canal de retorno `ADAT 2/3` (DI OUT do SYN-2). Resultado (alcança ou não) vai para `docs/camada-b-reamp.md` e para `music-setup/docs/equipamentos/presonus-quantum-hd8.md` (commit + push naquele repo).
- [ ] **Step 4:** commit + push.

---

### Task 11: Plugin, skill e docs finais

**Files:**
- Create: `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `skills/quantum-hd8/SKILL.md`
- Modify: `README.md`, `docs/protocol.md`

- [ ] **Step 1:** invocar `superpowers:writing-skills` (obrigatório para SKILL.md) e seguir o TDD de skill dele: cenário de baseline com subagente **sem** a skill ("sobe o ganho do canal 3 para 30 dB na HD 8"), anotar o que ele erra, então escrever a skill.
- [ ] **Step 2:** `SKILL.md`: quando usar (qualquer controle da HD 8), comandos, regras (um param novo por vez; `verified`; cena zera ganhos; nunca reiniciar daemon; saídas que chegam nos gabinetes pedem volume baixo antes).
- [ ] **Step 3:** `.claude-plugin/*.json` no formato de `../mvave/.claude-plugin/`, trocando nome/descrição/homepage.
- [ ] **Step 4:** README com a tabela de comandos e o que está `verified`.
- [ ] **Step 5:** `python3 -m pytest -q` (exit code 0) → commit + push.

---

## Self-review (18/09)

- Cobertura da spec: camada A (T3–T9), camada B (T10), CLI completa (T5, T7, T8, T9), segurança (Global Constraints + T7/T10), testes offline/device (todas), docs/skill (T11), efeito no rig no music-setup (T10). `tools/capture.py` da spec virou `listen --raw` (T6) + `tools/probe.py` (T3): mesma função, sem sniffer que exigiria sudo.
- Partes que dependem de medição (T4 formato, T5 forma da árvore, T6–T9) têm a hipótese escrita e o critério de troca: o `docs/protocol.md` medido prevalece.
